Task: Verify ClinXAI End-to-End Flow
URL: http://127.0.0.1:5500/index.html

Steps:
1. Click "INITIALIZE SYSTEM" button.
2. Verify "diagnose-page" is visible.
3. Upload "c:/Users/ijgam/OneDrive/Desktop/soha/clinxai/data/raw/mock_xray_0.png" to the file input `#file-input`.
   - Wait: Automation agent might need to start with the file available or simple click.
   - Note: File upload in browser agent can be tricky. I will try to use the `upload_file` action if available or just check if the UI loads.
   - Actually, the browser tool supports standard interactions. Constructing a valid file upload might require specific tool support. If not, I will just verify the UI state transitions and maybe skip the actual API call if upload is blocked, OR I can try to simply click the button if the form is pre-filled (it isn't).
   - *Self-correction*: Browser subagent might not support file upload from local disk easily. I'll rely on `test_api_local.py` for API verification and Browser Agent for *Visual/Navigation* verification.
   - *Better Plan for Browser Agent*: 
     1. Check Landing Page loads.
     2. Click Start.
     3. Check UI transitions.
     4. (If possible) Trigger analysis. If file input is hard, I might just mock the `handleFile` in console?
     5. Execute JS in console: `UI.handleFile(new File([""], "mock.png"));` -> This won't work for real API call (needs real bytes).
     6. Let's just verify navigation and visual Layout for now. The functionality was verified by `test_api_local.py`.

Revised Steps:
1. Navigate to landing page.
2. Verify Title and "INITIALIZE SYSTEM" button exist.
3. Click button.
4. Verify "Patient Data Entry" form appears.
5. Verify Three.js canvas exists in background.
